This directory creates a library to do pattern matching
for transl during the lookup and generate processes.
This is a regular expression search and replace facility.
It uses a data file to store the pattern matcher rules for
source and target.

The test directory contains a test application to excercise
this library. It uses data in the test/data subdirectory.
